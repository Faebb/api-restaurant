# Este archivo es solo para despliegue en la nube (Railway, Render, Fly.io).
# Para desarrollo local NO se necesita Docker — usa MySQL Workbench directamente.
# Ver README.md para instrucciones de setup local.
